[package]
name = "clippr-ui"
version = "0.1.0"
edition = "2024"
description = "Native GUI frontend for the clippr CLI"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
nightshade = { version = "0.6.70", features = ["egui", "file_dialog"] }
